# Player-Notifier-Updated
Updated Player notifier which checks player status and uses a discord webook to ping me when a player goes online.
I added a sleep function which pauses requests once rate limit is reached, fixed a memory leak, and added a field to adjust the rate at which you send requests
